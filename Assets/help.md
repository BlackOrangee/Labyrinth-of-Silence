# 📚 Документація: Система Завдань (Quest System)

## 📋 Зміст
1. [Огляд системи](#огляд-системи)
2. [Покрокове налаштування UI](#покрокове-налаштування-ui)
3. [Налаштування скриптів](#налаштування-скриптів)
4. [Налаштування звуків](#налаштування-звуків)
5. [Як працює система](#як-працює-система)
6. [API документація](#api-документація)
7. [Приклади використання](#приклади-використання)
8. [Troubleshooting](#troubleshooting)

---

## 🎯 Огляд системи

### Що нового додано:

**Нові скрипти:**
- `Quest.cs` - клас даних для одного завдання
- `QuestTracker.cs` - Singleton менеджер завдань
- `QuestUI.cs` - UI менеджер для відображення завдань

**Модифіковані скрипти:**
- `DoorController.cs` - додано OnTriggerExit та інтеграція з QuestTracker
- `SimpleInventory.cs` - додавання квесту при першому підборі ключа
- `CollectItem.cs` - оновлення прогресу квесту

### Що вміє система:

✅ Автоматичне додавання завдання "Зібрати всі ключі" після підбору першого ключа  
✅ Відображення завдань у лівому верхньому куті екрану  
✅ Оновлення прогресу у реальному часі (2/4, 3/4, тощо)  
✅ Викреслювання виконаних завдань з анімацією  
✅ Розчинення завдання після виконання  
✅ Звукові сигнали при оновленні та виконанні  
✅ Підказка "Потрібно знайти всі ключі" при підході до дверей без ключів  
✅ Popup біля дверей зникає коли гравець виходить з зони  

---

## 🎨 Покрокове налаштування UI

### Крок 1: Створення QuestTracker GameObject

1. **Відкрий свою сцену** (SampleScene або іншу)

2. **Створи порожній GameObject:**
   - Правий клік у Hierarchy → Create Empty
   - Назви його: `QuestTracker`

3. **Додай скрипт QuestTracker:**
   - Вибери GameObject `QuestTracker`
   - В Inspector натисни `Add Component`
   - Знайди та додай скрипт `QuestTracker`

4. **Налаштуй параметри QuestTracker:**
   ```
   Max Active Quests: 5
   Completed Quest Remove Delay: 2
   ```

5. **ВАЖЛИВО:** Не видаляй цей GameObject між сценами! Він автоматично стає DontDestroyOnLoad.

---

### Крок 2: Створення QuestPanel на Canvas

#### 2.1. Знайди Canvas

1. В Hierarchy знайди об'єкт `Canvas`
2. Якщо Canvas немає - створи:
   - Правий клік у Hierarchy → UI → Canvas
   - Unity автоматично додасть EventSystem

#### 2.2. Створення QuestPanel

1. **Створи панель для завдань:**
   - Правий клік на Canvas → UI → Panel
   - Назви: `QuestPanel`

2. **Налаштуй RectTransform для QuestPanel:**
   ```
   Anchors:
     Min: (0, 1)    ← лівий верхній кут
     Max: (0, 1)    ← лівий верхній кут
   
   Pivot: (0, 1)    ← точка прив'язки зліва зверху
   
   Position:
     Pos X: 20      ← відступ від лівого краю
     Pos Y: -20     ← відступ від верхнього краю
   
   Scale: (1, 1, 1)
   
   Width: 350
   Height: 200      ← буде автоматично змінюватись
   ```

3. **Налаштуй Image компонент QuestPanel:**
   ```
   Color: (0, 0, 0, 150)  ← напівпрозорий чорний фон
   ```
   Або встанови колір в hex: `#00000096`

4. **Додай Layout компоненти до QuestPanel:**
   - Вибери QuestPanel
   - Add Component → `Vertical Layout Group`
   - Налаштуй:
     ```
     Padding: Left=15, Right=15, Top=15, Bottom=15
     Spacing: 10
     Child Alignment: Upper Left
     Child Controls Size: ✓ Width, ✓ Height
     Child Force Expand: ✓ Width, ✗ Height
     ```
   
   - Add Component → `Content Size Fitter`
   - Налаштуй:
     ```
     Horizontal Fit: Unconstrained
     Vertical Fit: Preferred Size
     ```

#### 2.3. Створення QuestTitle (заголовок)

1. **Створи текстовий елемент:**
   - Правий клік на QuestPanel → UI → Text - TextMeshPro
   - Назви: `QuestTitle`

2. **Налаштуй TextMeshProUGUI:**
   ```
   Text: "ПОТОЧНІ ЗАВДАННЯ"
   
   Font: LiberationSans SDF
   Font Style: Bold
   Font Size: 20
   
   Color: RGB(230, 200, 180)  ← жовтувато-білий для horror
   
   Alignment: 
     Horizontal: Left
     Vertical: Top
   
   Auto Size: вимкнено
   ```

3. **Налаштуй RectTransform:**
   ```
   Height: 30
   (Width контролюється Layout Group)
   ```

#### 2.4. Створення QuestList (контейнер для завдань)

1. **Створи порожній GameObject:**
   - Правий клік на QuestPanel → Create Empty
   - Назви: `QuestList`

2. **Налаштуй RectTransform:**
   ```
   Anchors: Stretch (preset)
   Left: 0, Right: 0, Top: 0, Bottom: 0
   ```

3. **Додай Vertical Layout Group:**
   - Add Component → `Vertical Layout Group`
   - Налаштуй:
     ```
     Spacing: 8
     Child Alignment: Upper Left
     Child Controls Size: ✓ Width, ✓ Height
     Child Force Expand: ✓ Width, ✗ Height
     ```

---

### Крок 3: Створення QuestItem Prefab

#### 3.1. Створення базового QuestItem

1. **Створи порожній GameObject:**
   - Правий клік у Hierarchy (поза Canvas) → Create Empty
   - Назви: `QuestItem`

2. **Додай RectTransform:**
   - Add Component → Canvas → RectTransform
   ```
   Width: 320
   Height: 30
   ```

3. **Додай Layout Element:**
   - Add Component → `Layout Element`
   - Налаштуй:
     ```
     Preferred Height: 30
     ```

#### 3.2. Створення QuestText (текст завдання)

1. **Створи текстовий елемент:**
   - Правий клік на QuestItem → UI → Text - TextMeshPro
   - Назви: `QuestText`

2. **Налаштуй TextMeshProUGUI:**
   ```
   Text: "Зібрати всі ключі (0/4)"
   
   Font: LiberationSans SDF
   Font Style: Regular
   Font Size: 16
   
   Color: RGB(230, 230, 217)  ← світло-жовтуватий (active)
   
   Alignment:
     Horizontal: Left
     Vertical: Middle
   
   Wrapping: вимкнено
   Overflow: Overflow
   ```

3. **Налаштуй RectTransform:**
   ```
   Anchors: Stretch
   Left: 5, Right: 5, Top: 0, Bottom: 0
   ```

#### 3.3. Створення StrikeThrough (лінія викреслювання)

1. **Створи Image:**
   - Правий клік на QuestItem → UI → Image
   - Назви: `StrikeThrough`

2. **Налаштуй Image компонент:**
   ```
   Source Image: залиш None (або білий квадрат)
   Color: RGB(204, 51, 51)  ← темно-червоний (#CC3333)
   
   Image Type: Filled
   Fill Method: Horizontal
   Fill Origin: Left
   Fill Amount: 0  ← ВАЖЛИВО! Початково 0
   ```

3. **Налаштуй RectTransform:**
   ```
   Anchors: Stretch
   Left: 0, Right: 0
   Top: 0, Bottom: 0
   
   Height: 2  ← товщина лінії
   ```

4. **Відцентруй вертикально:**
   - В Inspector для RectTransform встанови:
   ```
   Pos Y: 0  (щоб була посередині)
   ```

#### 3.4. Створення префабу

1. **Створи папку для префабів** (якщо немає):
   - В Project → Assets → Prefabs
   - Правий клік → Create → Folder → назви "Prefabs"

2. **Перетягни QuestItem у папку Prefabs:**
   - Перетягни GameObject `QuestItem` з Hierarchy у папку Assets/Prefabs
   - Unity автоматично створить префаб

3. **Видали QuestItem з Hierarchy:**
   - Правий клік на QuestItem у Hierarchy → Delete
   - Префаб залишається у Project

---

### Крок 4: Налаштування QuestUI скрипта

#### 4.1. Додавання скрипта до Canvas

1. **Вибери Canvas:**
   - В Hierarchy клікни на Canvas

2. **Додай компонент QuestUI:**
   - В Inspector натисни Add Component
   - Знайди `QuestUI` та додай

#### 4.2. Прив'язка UI елементів

**ДУЖЕ ВАЖЛИВО:** Правильно прив'яжи всі посилання!

1. **Quest Panel Root:**
   - Перетягни `QuestPanel` з Hierarchy у поле `Quest Panel Root`

2. **Quest List Container:**
   - Розгорни QuestPanel у Hierarchy
   - Перетягни `QuestList` у поле `Quest List Container`

3. **Quest Item Prefab:**
   - З Project панелі (Assets/Prefabs)
   - Перетягни префаб `QuestItem` у поле `Quest Item Prefab`

#### 4.3. Налаштування параметрів

```
Звуки:
  Update Sound: (перетягни bell.mp3 з Assets)
  Complete Sound: (перетягни triumph.mp3 з Assets)
  Sound Volume: 0.7

Анімація:
  Fade In Duration: 0.5
  Strikethrough Duration: 0.5
  Fade Out Duration: 1.0

Кольори (Horror стиль):
  Active Quest Color: RGB(230, 230, 217)    ← світлий
  Completed Quest Color: RGB(102, 102, 102) ← темно-сірий
  Strikethrough Color: RGB(204, 51, 51)     ← червоний
```

**Як додати звукові файли:**
1. Знайди у Project папку Assets (де лежать bell.mp3 та triumph.mp3)
2. Перетягни bell.mp3 у поле `Update Sound`
3. Перетягни triumph.mp3 у поле `Complete Sound`

---

## 🔧 Налаштування скриптів

### Крок 5: Заміна старих скриптів

#### 5.1. DoorController.cs

1. **Знайди файл:**
   - У VSCode або Unity відкрий Assets/Scripts/DoorController.cs

2. **Заміни весь вміст файлу** на новий (з модифікованого файлу)

3. **Збережи файл** (Ctrl+S або Cmd+S)

#### 5.2. SimpleInventory.cs

1. **Знайди файл:**
   - Assets/Scripts/SimpleInventory.cs

2. **Заміни весь вміст файлу** на новий

3. **Збережи файл**

#### 5.3. CollectItem.cs

1. **Знайди файл:**
   - Assets/Scripts/CollectItem.cs

2. **Заміни весь вміст файлу** на новий

3. **Збережи файл**

#### 5.4. Додавання нових скриптів

**Скопіюй ці файли в Assets/Scripts/:**
- Quest.cs
- QuestTracker.cs
- QuestUI.cs

**Як це зробити:**
1. Відкрий папку Assets/Scripts у Project
2. Перетягни нові файли .cs з папки, де вони були згенеровані
3. Зачекай поки Unity скомпілює (дивись наProgress Bar знизу)

---

### Крок 6: Налаштування DoorController

1. **Знайди GameObject Door у Hierarchy**

2. **Вибери його та перевір Inspector:**
   ```
   Keys Required: 4
   Popup Manager: (має бути прив'язано UI_Manager)
   ```

3. **Переконайся що тригер налаштовано:**
   - Collider компонент:
     - Is Trigger: ✓ (має бути увімкнено)

---

## 🔊 Налаштування звуків

### Де взяти звукові файли:

У твоїй папці Assets є:
- `bell.mp3` - для оновлення квесту
- `triumph.mp3` - для завершення квесту

### Як додати звуки в QuestUI:

1. **Відкрий Canvas в Hierarchy**

2. **Вибери компонент QuestUI в Inspector**

3. **У секції "Звуки":**
   - Перетягни `bell.mp3` у поле **Update Sound**
   - Перетягни `triumph.mp3` у поле **Complete Sound**
   - Встанови **Sound Volume: 0.7** (або на свій смак)

### Як працюють звуки:

- 🔔 **bell.mp3** відтворюється при:
  - Додаванні нового квесту
  - Оновленні прогресу (зібрав ключ)

- 🎺 **triumph.mp3** відтворюється при:
  - Виконанні квесту (зібрав всі ключі)

---

## ⚙️ Як працює система

### Блок-схема роботи:

```
Гравець підбирає ПЕРШИЙ ключ
         ↓
SimpleInventory.AddItem()
         ↓
collectedKeys == 1?
         ↓ ТАК
QuestTracker.AddQuest("collect_keys", "Зібрати всі ключі", 1, 4)
         ↓
QuestTracker викликає OnQuestAdded
         ↓
QuestUI отримує подію та створює QuestItem
         ↓
Анімація появи (fade in + slide)
         ↓
Відтворюється bell.mp3
         ↓
[Завдання показано на екрані]


Гравець підбирає ще один ключ
         ↓
CollectItem.OnInteract()
         ↓
QuestTracker.UpdateQuest("collect_keys", 2)
         ↓
QuestTracker викликає OnQuestUpdated
         ↓
QuestUI оновлює текст: "Зібрати всі ключі (2/4)"
         ↓
Відтворюється bell.mp3


Гравець зібрав всі 4 ключі
         ↓
QuestTracker.UpdateQuest("collect_keys", 4)
         ↓
quest.IsTargetReached() == true
         ↓
QuestTracker.CompleteQuest("collect_keys")
         ↓
QuestTracker викликає OnQuestCompleted
         ↓
QuestUI запускає CompleteQuestAnimation():
  1. Змінює колір тексту на сірий
  2. Анімація викреслювання (0.5 сек)
  3. Пауза (0.3 сек)
  4. Fade out (1 сек)
  5. Видаляє QuestItem
         ↓
Відтворюється triumph.mp3
         ↓
Через 2 секунди квест видаляється з QuestTracker
```

### Логіка дверей:

```
Гравець підходить до дверей (OnTriggerEnter)
         ↓
Перевірка: є інвентар?
         ↓ ТАК
Скільки ключів зібрано?
         ↓
ВИПАДОК 1: collected < keysRequired
         ↓
Чи є квест "collect_keys"?
         ↓ НІ
Додати квест: "Потрібно знайти всі ключі (0/4)"
         ↓
Показати popup: "Зібрано ключів: 0 / 4" [кнопка неактивна]


ВИПАДОК 2: collected >= keysRequired
         ↓
Показати popup: "Зібрано ключів: 4 / 4" [Відчинити]
         ↓
Гравець натискає "Відчинити"
         ↓
OpenDoor() - двері відчиняються!


Гравець виходить з зони (OnTriggerExit)
         ↓
popupManager.HidePopup()
         ↓
Popup зникає ✅
```

---

## 📖 API Документація

### QuestTracker

#### Основні методи:

```csharp
// Додати нове завдання
QuestTracker.Instance.AddQuest(
    string questId,           // "collect_keys"
    string description,       // "Зібрати всі ключі"
    int currentProgress,      // 0
    int targetProgress        // 4
);

// Оновити прогрес
QuestTracker.Instance.UpdateQuest(
    string questId,           // "collect_keys"
    int newProgress          // 2
);

// Завершити завдання вручну
QuestTracker.Instance.CompleteQuest(
    string questId           // "collect_keys"
);

// Перевірити чи існує квест
bool exists = QuestTracker.Instance.HasQuest("collect_keys");

// Отримати квест
Quest quest = QuestTracker.Instance.GetQuest("collect_keys");

// Отримати прогрес
(int current, int target) = QuestTracker.Instance.GetQuestProgress("collect_keys");
// Приклад: current = 2, target = 4

// Отримати всі активні квести
List<Quest> quests = QuestTracker.Instance.GetAllQuests();

// Видалити квест
QuestTracker.Instance.RemoveQuest("collect_keys");

// Очистити всі квести (для дебагу)
QuestTracker.Instance.ClearAllQuests();
```

#### Події:

```csharp
// Підписка на події:
QuestTracker.Instance.OnQuestAdded += MyOnQuestAdded;
QuestTracker.Instance.OnQuestUpdated += MyOnQuestUpdated;
QuestTracker.Instance.OnQuestCompleted += MyOnQuestCompleted;

void MyOnQuestAdded(Quest quest)
{
    Debug.Log($"Додано: {quest.description}");
}

void MyOnQuestUpdated(Quest quest)
{
    Debug.Log($"Оновлено: {quest.GetFormattedText()}");
}

void MyOnQuestCompleted(Quest quest)
{
    Debug.Log($"Виконано: {quest.description}");
}
```

---

### Quest

#### Властивості:

```csharp
string questId;              // Унікальний ID
string description;          // Опис завдання
int currentProgress;         // Поточний прогрес
int targetProgress;          // Ціль
bool isCompleted;            // Чи виконано
float createdTime;           // Час створення
```

#### Методи:

```csharp
// Отримати форматований текст
string text = quest.GetFormattedText();
// Повертає: "Зібрати всі ключі (2/4)"

// Перевірити чи досягнуто цілі
bool reached = quest.IsTargetReached();

// Оновити прогрес
quest.UpdateProgress(3);
```

---

### QuestUI

#### Налаштування в Inspector:

```csharp
// UI Елементи
public GameObject questPanelRoot;
public Transform questListContainer;
public GameObject questItemPrefab;

// Звуки
public AudioClip updateSound;      // bell.mp3
public AudioClip completeSound;    // triumph.mp3
public float soundVolume = 0.7f;

// Анімація
public float fadeInDuration = 0.5f;
public float strikethroughDuration = 0.5f;
public float fadeOutDuration = 1f;

// Кольори
public Color activeQuestColor;
public Color completedQuestColor;
public Color strikethroughColor;
```

#### Тестові методи (Context Menu):

```csharp
// В Inspector на компоненті QuestUI:
// Правий клік → "Test: Add Quest"
// Додає тестове завдання

// Правий клік → "Test: Update First Quest"
// Оновлює перше завдання зі списку
```

---

## 💡 Приклади використання

### Приклад 1: Додати квест збору документів

```csharp
// У скрипті де потрібно додати квест:
void Start()
{
    if (QuestTracker.Instance != null)
    {
        QuestTracker.Instance.AddQuest(
            "collect_documents",           // ID
            "Знайти всі документи",       // Опис
            0,                             // Поточний прогрес
            5                              // Потрібно всього
        );
    }
}

// При підборі документа:
void OnDocumentCollected()
{
    documentsCollected++;
    QuestTracker.Instance?.UpdateQuest("collect_documents", documentsCollected);
}
```

### Приклад 2: Квест з автозавершенням

```csharp
// Коли гравець увійшов в певну зону:
void OnTriggerEnter(Collider other)
{
    if (other.CompareTag("Player"))
    {
        // Додаємо квест "Дослідити підвал"
        QuestTracker.Instance?.AddQuest(
            "explore_basement",
            "Дослідити підвал",
            0,
            1
        );
        
        // Одразу виконуємо
        QuestTracker.Instance?.CompleteQuest("explore_basement");
    }
}
```

### Приклад 3: Квест з підписом на події

```csharp
void Start()
{
    // Підписуємось на завершення квесту
    if (QuestTracker.Instance != null)
    {
        QuestTracker.Instance.OnQuestCompleted += OnAnyQuestCompleted;
    }
}

void OnAnyQuestCompleted(Quest quest)
{
    // Коли будь-який квест виконано
    if (quest.questId == "collect_keys")
    {
        // Відкрити секретну кімнату
        secretDoor.SetActive(true);
    }
}

void OnDestroy()
{
    // Не забувай відписуватись!
    if (QuestTracker.Instance != null)
    {
        QuestTracker.Instance.OnQuestCompleted -= OnAnyQuestCompleted;
    }
}
```

### Приклад 4: Динамічний квест

```csharp
// Змінна кількість предметів:
int totalEnemies = 10;

void Start()
{
    QuestTracker.Instance?.AddQuest(
        "defeat_enemies",
        "Знищити всіх ворогів",
        0,
        totalEnemies
    );
}

void OnEnemyDefeated()
{
    enemiesDefeated++;
    QuestTracker.Instance?.UpdateQuest("defeat_enemies", enemiesDefeated);
}
```

---

## 🐛 Troubleshooting

### Проблема 1: Квест не з'являється на екрані

**Можливі причини:**

1. ❌ **QuestUI не прив'язано до Canvas**
   - **Рішення:** Перевір що скрипт QuestUI доданий до Canvas
   - В Hierarchy → Canvas → Inspector → шукай компонент QuestUI

2. ❌ **Не прив'язані UI елементи**
   - **Рішення:** Відкрий Canvas → QuestUI компонент
   - Перевір що всі поля заповнені:
     - Quest Panel Root → QuestPanel
     - Quest List Container → QuestList
     - Quest Item Prefab → префаб QuestItem

3. ❌ **QuestPanel вимкнено**
   - **Рішення:** В Hierarchy знайди QuestPanel
   - Переконайся що галочка ліворуч УВІМКНЕНА

4. ❌ **QuestTracker не існує**
   - **Рішення:** Створи GameObject "QuestTracker" з компонентом QuestTracker
   - Або запусти гру - він створиться автоматично

---

### Проблема 2: Звуки не відтворюються

**Можливі причини:**

1. ❌ **Звукові файли не прив'язані**
   - **Рішення:** Canvas → QuestUI → Звуки
   - Перетягни bell.mp3 та triumph.mp3

2. ❌ **Гучність на 0**
   - **Рішення:** Перевір Sound Volume > 0

3. ❌ **AudioListener відсутній**
   - **Рішення:** Main Camera має містити AudioListener компонент
   - Add Component → Audio → Audio Listener

---

### Проблема 3: Popup біля дверей не зникає

**Можливі причини:**

1. ❌ **Старий DoorController без OnTriggerExit**
   - **Рішення:** Заміни DoorController.cs на НОВИЙ файл
   - Збережи та перезапусти Unity

2. ❌ **Collider дверей не є тригером**
   - **Рішення:** Door → Collider → Is Trigger: ✓

3. ❌ **Гравець не має тег "Player"**
   - **Рішення:** Вибери Player GameObject
   - Inspector → Tag → встанови "Player"

---

### Проблема 4: Текст квесту обрізається

**Можливі причини:**

1. ❌ **QuestPanel занадто вузький**
   - **Рішення:** QuestPanel → RectTransform → Width: 350 або більше

2. ❌ **Content Size Fitter не працює**
   - **Рішення:** QuestPanel → Add Component → Content Size Fitter
   - Vertical Fit: Preferred Size

3. ❌ **Layout Group налаштований неправильно**
   - **Рішення:** QuestPanel → Vertical Layout Group
   - Child Controls Size: Width ✓, Height ✓

---

### Проблема 5: Анімація не плавна

**Можливі причини:**

1. ❌ **Низький FPS**
   - **Рішення:** Перевір Performance в Profiler

2. ❌ **Тривалість анімації занадто коротка**
   - **Рішення:** Canvas → QuestUI → Анімація
   - Збільш Fade In Duration до 0.5-1.0

---

### Проблема 6: Квест не оновлюється після підбору ключа

**Можливі причини:**

1. ❌ **Старий CollectItem без інтеграції**
   - **Рішення:** Заміни CollectItem.cs на новий

2. ❌ **Назва предмета не містить "ключ"**
   - **Рішення:** В CollectItem → Display Name має бути:
   - "Ключ(червоний)" або "Key(red)" тощо

3. ❌ **SimpleInventory старий**
   - **Рішення:** Заміни SimpleInventory.cs на новий

---

### Проблема 7: Помилка "QuestTracker.Instance is null"

**Можливі причини:**

1. ❌ **QuestTracker не створено**
   - **Рішення:** Створи GameObject з компонентом QuestTracker
   - Або просто запусти гру - він створиться автоматично

2. ❌ **Викликається до Awake/Start**
   - **Рішення:** Використовуй перевірку:
   ```csharp
   if (QuestTracker.Instance != null)
   {
       QuestTracker.Instance.AddQuest(...);
   }
   ```

---

### Проблема 8: Префаб QuestItem не створюється

**Можливі причини:**

1. ❌ **Префаб не прив'язано**
   - **Рішення:** Canvas → QuestUI → Quest Item Prefab
   - Перетягни префаб з Assets/Prefabs/QuestItem

2. ❌ **Префаб зіпсований**
   - **Рішення:** Видали старий префаб
   - Створи новий за інструкцією у Крок 3

---

### Проблема 9: Викреслювання не працює

**Можливі причини:**

1. ❌ **StrikeThrough Image відсутній**
   - **Рішення:** Відкрий префаб QuestItem
   - Додай дочірній Image з назвою "StrikeThrough"

2. ❌ **Fill Amount не 0**
   - **Рішення:** StrikeThrough → Image
   - Image Type: Filled
   - Fill Amount: 0

3. ❌ **Колір StrikeThrough прозорий**
   - **Рішення:** Color → Alpha: 255 (непрозорий)

---

### Проблема 10: Квест додається двічі

**Можливі причини:**

1. ❌ **Логіка викликається кілька разів**
   - **Рішення:** Використовуй HasQuest:
   ```csharp
   if (!QuestTracker.Instance.HasQuest("collect_keys"))
   {
       QuestTracker.Instance.AddQuest(...);
   }
   ```

---

## 🎓 Додаткові поради

### Оптимізація:

1. **Обмеж кількість активних квестів:**
   ```
   QuestTracker → Max Active Quests: 5
   ```

2. **Зменш час затримки видалення:**
   ```
   QuestTracker → Completed Quest Remove Delay: 1.5
   ```

3. **Прискор анімацію:**
   ```
   QuestUI → Fade In Duration: 0.3
   QuestUI → Fade Out Duration: 0.7
   ```

### Стилізація для Horror:

1. **Використовуй темні кольори:**
   - Фон QuestPanel: #000000AA (чорний, альфа 170)
   - Текст: #E6C8B4 (світло-коричневий)

2. **Додай ефект тіні:**
   - QuestText → Add Component → Shadow
   - Effect Distance: (2, -2)
   - Color: чорний

3. **Використовуй distressed шрифт:**
   - Замість LiberationSans можна використати:
   - Creepster, Nosifer, або інший horror шрифт

### Дебаг поради:

1. **Використовуй Context Menu:**
   - Правий клік на QuestTracker → "Debug: Show All Quests"
   - Показує всі активні квести в Console

2. **Тестуй квести в Editor:**
   - QuestUI → Test: Add Quest
   - QuestUI → Test: Update First Quest

3. **Перевіряй логи:**
   - Усі важливі дії логуються в Console
   - Шукай "QuestTracker:", "QuestUI:", "CollectItem:"

---

## ✅ Чеклист перед запуском

Перед тим як запустити гру, перевір:

### UI:
- [ ] Canvas існує
- [ ] QuestPanel створено та налаштовано
- [ ] QuestTitle додано з правильним текстом
- [ ] QuestList створено з Vertical Layout Group
- [ ] Префаб QuestItem створено правильно
- [ ] QuestText у префабі налаштовано
- [ ] StrikeThrough у префабі налаштовано (Fill Amount = 0)
- [ ] Префаб збережено в Assets/Prefabs

### Скрипти:
- [ ] Quest.cs доданий в Assets/Scripts
- [ ] QuestTracker.cs доданий в Assets/Scripts
- [ ] QuestUI.cs доданий в Assets/Scripts
- [ ] DoorController.cs ЗАМІНЕНО на новий
- [ ] SimpleInventory.cs ЗАМІНЕНО на новий
- [ ] CollectItem.cs ЗАМІНЕНО на новий
- [ ] Всі скрипти скомпільовані без помилок

### Прив'язки:
- [ ] QuestTracker GameObject створено
- [ ] Canvas має компонент QuestUI
- [ ] QuestUI → Quest Panel Root прив'язано
- [ ] QuestUI → Quest List Container прив'язано
- [ ] QuestUI → Quest Item Prefab прив'язано
- [ ] QuestUI → Update Sound прив'язано (bell.mp3)
- [ ] QuestUI → Complete Sound прив'язано (triumph.mp3)

### Об'єкти:
- [ ] Player має тег "Player"
- [ ] Player має SimpleInventory компонент
- [ ] Door має DoorController компонент
- [ ] Door має Collider з Is Trigger = true
- [ ] Всі ключі мають CollectItem компонент
- [ ] Display Name ключів містить слово "ключ" або "key"

---

## 🚀 Готово!

Якщо все налаштовано правильно, тепер:

1. ✅ При підборі **першого ключа** з'явиться завдання "Зібрати всі ключі (1/4)"
2. ✅ При підборі кожного наступного ключа - прогрес оновиться (2/4, 3/4, 4/4)
3. ✅ Лунатиме **bell.mp3** при кожному оновленні
4. ✅ При досягненні 4/4 - квест викреслиться та зникне
5. ✅ Лунатиме **triumph.mp3** при завершенні
6. ✅ При підході до дверей БЕЗ ключів - з'явиться підказка "Потрібно знайти всі ключі (0/4)"
7. ✅ При підході до дверей З ключами - popup "Зібрано ключів: 4/4 [Відчинити]"
8. ✅ При виході від дверей - popup **зникне автоматично** ✨

---

**Успіхів з розробкою! 🎮👻**

Якщо щось не працює - дивись розділ Troubleshooting або пиши мені!
